spring:
  datasource:
    url: jdbc:h2:mem:team-test;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: WARN
    org.bakhtaoui: DEBUG
